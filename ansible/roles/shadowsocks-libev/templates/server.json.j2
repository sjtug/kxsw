{
    "server": ["[::0]", "0.0.0.0"],
    "server_port": {{ ss_server_port }},
    "password": "{{ ss_password }}",
    "method": "{{ ss_encryption_method }}",
    "timeout": {{ ss_timeout }},
    "mode": "tcp_and_udp",
{% if 'scope' in ansible_default_ipv6 and ansible_default_ipv6.scope == 'global' %}
    "ipv6_first": {{ ss_ipv6_first }},
{% endif %}
{% if ss_fast_open is defined and ansible_kernel | version_compare('3.7', '>=') %}
    "fast_open": {{ ss_fast_open }}
{% endif %}
}
